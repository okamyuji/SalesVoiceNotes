name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  security:
    permissions:
      contents: read
    uses: okamyuji/reusable-workflows/.github/workflows/security-scan.yml@v1
